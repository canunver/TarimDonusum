@using Microsoft.Extensions.Localization
@inject IStringLocalizer<TarimDonusum.SharedResource> L
@inject Microsoft.AspNetCore.Hosting.IWebHostEnvironment Env

@{
    Layout = "~/Views/Shared/_HomeLayout.cshtml";
    ViewData["Title"] = L["Home.Giris"];
}

<style>
    .login-card {
        background: rgba(255,255,255,.55) !important;
        backdrop-filter: blur(2px);
        -webkit-backdrop-filter: blur(4px);
        box-shadow: 0 10px 30px rgba(0,0,0,.25);
    }

        .login-card .card-header {
            background: rgba(0,123,255,.45) !important;
            color: white;
        }

        .login-card .card-body,
        .login-card .card-footer {
            background: rgba(255,255,255,.35) !important;
        }

    .login-center {
        min-height: calc(100vh - 170px);
        display: flex;
        align-items: center;
    }

    .verification-timer-box {
        min-width: 58px;
        text-align: center;
        font-variant-numeric: tabular-nums;
    }

    .captcha-group .form-control,
    .captcha-group .input-group-text,
    .captcha-group .btn {
        height: 44px;
        background: #eef3ff;
    }

    .captcha-group .form-control {
        min-height: 44px;
        border-color: #ced4da;
        box-shadow: none;
    }

    .captcha-group .input-group-text {
        border-color: #ced4da;
        overflow: hidden;
        background: #fff;
    }

    .captcha-group .captcha-image {
        width: 142px;
        height: 42px;
        object-fit: contain;
        cursor: pointer;
        background: #fff;
    }

    .captcha-group .btn {
        width: 48px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: #4f5b66;
        border-color: #ced4da;
    }
</style>
<div id="ajaxMesaj"
     class="alert alert-info alert-dismissible floating-message"
     style="display:@(TempData["Mesaj"] != null ? "block" : "none")">
    <span id="ajaxMesajMetin">@TempData["Mesaj"]</span>
    <button type="button" id="ajaxMesajKapat" class="close" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>

<div class="row justify-content-center login-center">
    <div class="col-12 col-sm-10 col-md-6 col-lg-5">
        <div class="card card-primary login-card">
            <div class="card-header">
                <h3 class="card-title">@L["Home.CardTitle"]</h3>
            </div>


            <div id="divLogin" class="card-body">
                <div class="form-group">
                    <label>@L["Home.KulKod"]</label>
                    <input type="text" id="KulKod" class="form-control" />
                </div>

                <div class="form-group">
                    <label>@L["Home.Sifre"]</label>
                    <input type="password" id="Sifre" class="form-control" />
                </div>

                <div class="form-group">
                    <label>@L["Home.Captcha"]</label>
                    <div class="input-group captcha-group">
                        <div class="input-group-prepend">
                            <span class="input-group-text p-0">
                                <img id="captchaImg"
                                     src="/Captcha/Resim?t=@DateTime.Now.Ticks"
                                     class="captcha-image" />
                            </span>
                        </div>

                        <input type="text" id="Captcha" class="form-control" autocomplete="off" />

                        <div class="input-group-append">
                            <button type="button" id="btnCaptchaYenile" class="btn btn-outline-secondary">
                                <i class="fas fa-sync-alt"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <div id="divDogrulama" class="card-body" style="display:none;">
                <div class="alert alert-info">
                    @L["Home.DogrulamaBilgi"]
                </div>

                <div class="form-group">
                    <label>@L["Home.DogrulamaKodu"]</label>

                    <div class="input-group">
                        <input type="text"
                               id="DogrulamaKodu"
                               class="form-control"
                               autocomplete="off" />

                        <div class="input-group-append">
                            <button type="button"
                                    id="btnKodYenile"
                                    class="btn btn-outline-primary">
                                <i class="fas fa-sync-alt"></i> @L["Home.Button.KoduYenile"]
                            </button>

                            <span id="loginKodSayac"
                                  class="input-group-text verification-timer-box text-muted"
                                  data-kalan-saniye="0"></span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card-footer">
                <button type="button" id="btnLogin1" class="btn btn-primary">
                    <i class="fas fa-sign-in-alt"></i> @L["Home.Button.Giris"]
                </button>

                <button type="button" id="btnLogin2" class="btn btn-success" style="display:none;">
                    <i class="fas fa-check"></i> @L["Home.Button.DogrulaGirisYap"]
                </button>

                @if (Env.IsDevelopment())
                {
                    <a class="btn btn-outline-info float-right" asp-controller="Home" asp-action="TestBasvuru">
                        <i class="fas fa-vial"></i> Test Başvuruya Geç
                    </a>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        let loginKodTimer = null;
        const txtKodSuresiBitti = "@L["Home.KodSuresiBitti"]";
        const txtIslemHatasi = "@L["Home.Hata.IslemSirasindaHata"]";

        function captchaYenile() {
            $("#captchaImg").attr("src", "/Captcha/Resim?t=" + new Date().getTime());
        }

        function sureMetni(saniye) {
            var dakika = Math.floor(saniye / 60);
            var kalanSaniye = saniye % 60;

            return dakika.toString().padStart(2, "0") + ":" +
                   kalanSaniye.toString().padStart(2, "0");
        }

        function loginSayaciBaslat(kalanSaniye) {
            if (loginKodTimer) {
                window.clearTimeout(loginKodTimer);
                loginKodTimer = null;
            }

            $("#loginKodSayac")
                .removeClass("text-danger")
                .addClass("text-muted")
                .text("");

            $("#DogrulamaKodu")
                .val("")
                .prop("readonly", false);

            $("#btnLogin2").prop("disabled", false);
            $("#btnKodYenile").prop("disabled", false);

            function guncelle() {
                if (kalanSaniye <= 0) {
                    $("#loginKodSayac")
                        .removeClass("text-muted")
                        .addClass("text-danger")
                        .text(txtKodSuresiBitti);

                    $("#DogrulamaKodu").prop("readonly", true);
                    $("#btnLogin2").prop("disabled", true);
                    $("#btnKodYenile").prop("disabled", false);

                    return;
                }

                $("#loginKodSayac")
                    .removeClass("text-danger")
                    .addClass("text-muted")
                    .text(sureMetni(kalanSaniye));

                kalanSaniye--;
                loginKodTimer = window.setTimeout(guncelle, 1000);
            }

            guncelle();
        }
        function loginData() {
            return {
                kulKod: $("#KulKod").val(),
                sifre: $("#Sifre").val(),
                captcha: $("#Captcha").val()
            };
        }

        function dogrulamaKoduGonder() {
            $.ajax({
                url: "/Home/Login1",
                type: "POST",
                data: loginData(),
                dataType: "json"
            })
            .done(function (cevap) {
                PopupMesajGoster(cevap.mesaj, cevap.basarili);

                if (!cevap.basarili) {
                    captchaYenile();
                    return;
                }

                $("#divLogin").hide();
                $("#btnLogin1").hide();

                $("#divDogrulama").show();
                $("#btnLogin2").show();

                $("#DogrulamaKodu")
                    .val("")
                    .prop("readonly", false)
                    .focus();

                loginSayaciBaslat(cevap.kalanSaniye || 180);
            })
            .fail(function () {
                PopupMesajGoster(txtIslemHatasi, false);
            });
        }

        $("#btnLogin1").on("click", dogrulamaKoduGonder);

        $("#btnKodYenile").on("click", function () {
            dogrulamaKoduGonder();
        });

        $("#btnLogin2").on("click", function () {
            var data = loginData();
            data.dogrulamaKodu = $("#DogrulamaKodu").val();

            $.ajax({
                url: "/Home/Login2",
                type: "POST",
                data: data,
                dataType: "json"
            })
            .done(function (cevap) {
                PopupMesajGoster(cevap.mesaj, cevap.basarili);

                if (!cevap.basarili)
                    return;

                window.location.href = cevap.redirectUrl;
            })
            .fail(function () {
                PopupMesajGoster(txtIslemHatasi, false);
            });
        });

        $("#btnCaptchaYenile").on("click", captchaYenile);
        $("#captchaImg").on("click", captchaYenile);

        $(function () {
            PopupMesajIlklendir();
        });
    </script>
}
